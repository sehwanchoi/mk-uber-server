type Place {
    id: Int!
    name: String!
    lat: Float!
    lng: Float! 
    address: String!
    isFavorite: Boolean!
    user: User!
    userId: Int!
    createdAt: String!
    updatedAt: String!
}

type Query {
    place: Place 
}

